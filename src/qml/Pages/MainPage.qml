import QtQuick
import QtQuick.Window
import QtQuick.Controls
import "../Components/"

Page {
    id: pageOne
    header: AppHeader {
        id: appHeader
    }

    footer: AppFooter {
        id: appFooter
    }
}
